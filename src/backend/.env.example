# Todo Backend Environment Configuration
# Copy this file to .env and fill in your values

# LLM Provider: "groq" (free) or "openai" (paid)
# Auto-detects from available keys if not set.
# LLM_PROVIDER=groq

# Groq (free tier — get key at https://console.groq.com)
GROQ_API_KEY=your-groq-api-key-here
# GROQ_MODEL=llama-3.3-70b-versatile

# OpenAI (paid — requires billing at https://platform.openai.com)
OPENAI_API_KEY=your-openai-api-key-here

# Authentication - Secret key for JWT token verification (must match frontend's BETTER_AUTH_SECRET)
BETTER_AUTH_SECRET=your-better-auth-secret-here

# Database - Connection URL
# Development (SQLite): sqlite:///./todo.db
# Production (Neon PostgreSQL): postgresql://user:password@host:port/database?sslmode=require
DATABASE_URL=sqlite:///./todo.db

# Optional: Production database URL (Neon PostgreSQL) — fallback if DATABASE_URL not set
NEON_DB_URL=

# CORS - Frontend origin for cross-origin requests
FRONTEND_ORIGIN=http://localhost:3000

# Additional CORS origins (comma-separated, e.g., https://myapp.vercel.app,https://other.com)
CORS_ORIGINS=

# Server - Host and port for uvicorn
HOST=0.0.0.0
PORT=8000
