apiVersion: v2
name: todo-chatbot
description: >
  Todo AI Chatbot — FastAPI backend (with embedded MCP stdio subprocess)
  + React/Vite frontend. Deploys to local Minikube cluster.
type: application
version: 0.1.0
appVersion: "2.0.0"
